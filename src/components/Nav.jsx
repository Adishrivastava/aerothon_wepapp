import React from 'react';

const Nav = (props) => {
	// const navigate = useNavigate();
	return (
		<div className="navbar">
			<a href="/">
				<div className="title">Nhp</div>
			</a>
		</div>
	);
};

export default Nav;
